name: 🐳 Build And Push Docker Image
on:
  workflow_call:
    inputs:
      tag:
        type: string
        description: The tag to push to the Docker registry.
        # required: true
        # default: latest

jobs:
  build:
    name: 🐳 Build
    # only build/deploy main branch on pushes
    if: ${{ (github.ref == 'refs/heads/develop' || github.ref == 'refs/heads/ope') && github.event_name == 'push' }}
    runs-on: ubuntu-latest
    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.12.1

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v3

      - name: 🧑‍💻 Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          logout: true

      - name: 🐳 Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # Setup cache
      #on ajout du cache pour que le premier build soit lent et les autres très rabide
      - name: ⚡️ Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # build production image
      - name: 🐳 Build Production Image
        if: ${{ github.ref == 'refs/heads/ope' }}
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: josephaxelad/gdo-front:ope
          build-args: |
            COMMIT_SHA=${{ github.sha }} \
            APP_ENV='ope'\
          #  APP_ENV=${{ secrets.APP_ENV }} \

          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-new

      # build satging image
      - name: 🐳 Build Staging Image
        if: ${{ github.ref == 'refs/heads/develop' }}
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: josephaxelad/gdo-front:latest
          build-args: |
            COMMIT_SHA=${{ github.sha }} \
            APP_ENV='re7' \
          #  APP_ENV=${{ secrets.APP_ENV }} \

          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,mode=max,dest=/tmp/.buildx-cache-new


          # This ugly bit is necessary if you don't want your cache to grow forever
      # till it hits GitHub's limit of 5GB.
      # Temp fix
      # https://github.com/docker/build-push-action/issues/252
      # https://github.com/moby/buildkit/issues/1896
      - name: 🚚 Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
